DATABASE_URL="file:./dev.db"
# For PostgreSQL in production, switch `provider` to "postgresql" in `prisma/schema.prisma`
# and set DATABASE_URL to a Postgres connection string, e.g.
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DB?schema=public"

